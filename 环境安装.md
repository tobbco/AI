# 环境安装
## 基础环境
1. Anaconda3 python集成环境。
2. pytorch  非Nvidia显卡且不足16G建议安装cpu版本
   ```bash
   nvidia-smi #查看支持的cuda版本。
   ```
3. pycharm  编码IDEA

## Huggingface 包
```bash
pip install transformers datasets tokenizers
```

